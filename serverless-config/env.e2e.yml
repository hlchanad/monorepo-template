AWS_ACCOUNT_ID: "000000000000"

# Mock AWS Infrastructure
AWS_ACCESS_KEY_ID: test
AWS_SECRET_ACCESS_KEY: test
AWS_REGION: ap-northeast-1
LOCALSTACK_PORT: 4567
AWS_ENDPOINT_URL: http://localhost:4567

# Master Alias Link (Local Environment)
SITE_ALIAS_DOMAIN: localhost
ACM_CERTIFICATE_ARN_GLOBAL: ''
DOMAIN_ENABLED: false

# App Environment (Isolated from AWS_ reserved keys)
VARS:
  SITE_ALIAS_DOMAIN: ${self:custom.config.SITE_ALIAS_DOMAIN}
  SITE_BUCKET: monorepo-template-e2e-web
