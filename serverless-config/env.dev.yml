AWS_ACCOUNT_ID: ${aws:accountId}
AWS_REGION: ap-northeast-1
SITE_ALIAS_DOMAIN: 'template-dev.example.com'
DOMAIN_ENABLED: true
ACM_CERTIFICATE_ARN_GLOBAL: arn:aws:acm:us-east-1:${aws:accountId}:certificate/REPLACE_ME
VARS:
  SITE_BUCKET: monorepo-template-dev-web
