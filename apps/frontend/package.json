{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "type-check": "tsc --noEmit -p tsconfig.json",
    "lint": "echo 'no lint for frontend'",
    "test": "echo 'no tests for frontend'"
  },
  "dependencies": {
    "@template/api-types": "^0.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.1.4",
    "vite": "^7.3.1"
  }
}
